<Page x:Name="Panel" x:Class="CustomDesktopShell.WeatherPanel"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CustomDesktopShell"
      mc:Ignorable="d" 
      d:DesignHeight="455" d:DesignWidth="1920"
      Title="WeatherPanel">

    <Page.Resources>
        <Style TargetType="{x:Type Button}">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Style.Resources>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="12,12,12,12" />

            <Setter Property="BorderBrush" Value="#3F707070" />
            <Setter Property="Background" >
                <Setter.Value>
                    <LinearGradientBrush EndPoint="1,1" StartPoint="0,0">
                        <GradientStop Color="#BA292929" Offset="1"/>
                        <GradientStop Color="#57AFAFAF" Offset="0"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="AliceBlue" />
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value=" GOST Type BU"/>
            <Setter Property="FontSize" Value="34"/>
        </Style>

        <!-- universal scrollbar style -->
        <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="rectangle"
                            SnapsToDevicePixels="True"
                            Background="White"
                            CornerRadius="3"
                            Height="{TemplateBinding Height}"
                            Width="{TemplateBinding Width}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
            <Setter Property="Width" Value="14"/>
            <Setter Property="Margin" Value="-14 0 0 0" />
            <Setter Property="MinWidth" Value="{Binding Width, RelativeSource={RelativeSource Self}}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="Bg" SnapsToDevicePixels="true" Background="Transparent">
                            <Border Padding="0 4">
                                <Track x:Name="PART_Track"
                           IsDirectionReversed="true"
                           IsEnabled="{TemplateBinding IsMouseOver}"
                           Width="6"
                           HorizontalAlignment="Center">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Opacity="0" Command="{x:Static ScrollBar.PageUpCommand}" />
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Opacity="0" Command="{x:Static ScrollBar.PageDownCommand}" />
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource ScrollBarThumb}" />
                                    </Track.Thumb>
                                </Track>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="MinWidth" Value="0"/>
                    <Setter Property="Height" Value="14"/>
                    <Setter Property="MinHeight" Value="{Binding Height, RelativeSource={RelativeSource Self}}"/>
                    <Setter Property="Margin" Value="0 -14 0 0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                <Grid x:Name="Bg" SnapsToDevicePixels="true">
                                    <Track x:Name="PART_Track"
                                IsEnabled="{TemplateBinding IsMouseOver}"
                                Height="6"
                                   >
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Opacity="0" Command="{x:Static ScrollBar.PageLeftCommand}"/>
                                        </Track.DecreaseRepeatButton>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Opacity="0" Command="{x:Static ScrollBar.PageRightCommand}"/>
                                        </Track.IncreaseRepeatButton>
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ScrollBarThumb}" />
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Rectangle x:Name="Corner" Grid.Column="1" Fill="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" Grid.Row="1"/>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" CanContentScroll="{TemplateBinding CanContentScroll}" CanHorizontallyScroll="False" CanVerticallyScroll="False" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" Grid.Column="0" Margin="{TemplateBinding Padding}" Grid.Row="0"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar" 
                               Opacity="0.2"
                               AutomationProperties.AutomationId="VerticalScrollBar" 
                               Cursor="Arrow" 
                               Grid.Column="1"
                               Maximum="{TemplateBinding ScrollableHeight}" 
                               Minimum="0" 
                               Grid.Row="0" 
                               Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                               Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                               ViewportSize="{TemplateBinding ViewportHeight}"/>
                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                               Opacity="0.2"
                               AutomationProperties.AutomationId="HorizontalScrollBar"
                               Cursor="Arrow" 
                               Grid.Column="0"
                               Maximum="{TemplateBinding ScrollableWidth}"
                               Minimum="0" 
                               Orientation="Horizontal"
                               Grid.Row="1" 
                               Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                               Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" 
                               ViewportSize="{TemplateBinding ViewportWidth}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation To="1" Duration="0:0:0.3" Storyboard.TargetName="PART_VerticalScrollBar" Storyboard.TargetProperty="Opacity" />
                                        <DoubleAnimation To="1" Duration="0:0:0.3" Storyboard.TargetName="PART_HorizontalScrollBar" Storyboard.TargetProperty="Opacity" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation To="0.2" Duration="0:0:0.3" Storyboard.TargetName="PART_VerticalScrollBar" Storyboard.TargetProperty="Opacity" />
                                        <DoubleAnimation To="0.2" Duration="0:0:0.3" Storyboard.TargetName="PART_HorizontalScrollBar" Storyboard.TargetProperty="Opacity" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Page.Resources>

    <Grid x:Name="MainGrid" Background="#F9101010">
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition MaxHeight="188" Height="8*"/>
            <RowDefinition MaxHeight="168" Height="8*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition MaxWidth="116" Width="1*"/>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition MaxWidth="116" Width="1*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="1" Grid.Column="1" CornerRadius="8" Margin="2" Background="#FF212121"></Border>
        <Border Grid.Row="2" Grid.Column="1" CornerRadius="8" Margin="2" Background="#FF212121"></Border>

        <Grid x:Name="SlidersGrid" Grid.Row="2" Grid.Column="1">
            <Grid.Resources>
                <Style TargetType="{x:Type Label}">
                    <Setter Property="FontSize" Value="16"/>
                </Style>
            </Grid.Resources>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="11*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>

            <Label x:Name="WindLabel" Content="Ветер" Grid.Row="0" HorizontalAlignment="Center" Foreground="White" VerticalAlignment="Center"/>
            <Label x:Name="TimeOfDayLabel" Content="Время суток" Grid.Row="1" HorizontalAlignment="Center" Foreground="White" VerticalAlignment="Center"/>
            <Slider x:Name="WindSlider" Grid.Column="1" Grid.Row="0" VerticalAlignment="Center" Maximum="200"/>
            <Slider x:Name="TimeOfDaySlider" Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" Maximum="255"></Slider>
            <Image x:Name="WindImage" Source="/Images/weather/wind_pic.png" Grid.Row="0" Grid.Column="2" MaxHeight="32"/>
            <Image x:Name="TimeOfDayImage" Source="/Images/weather/sun_pic.png" Grid.Row="1" Grid.Column="2" MaxHeight="32"/>

        </Grid>

        <Grid x:Name="ButtonsGrid" Grid.Column="1" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="SunButton" Grid.Column="0">
                <Button.Content>
                    <Viewbox MaxHeight="128">
                        <StackPanel>
                            <Image Source="/Images/weather/sun_pic.png" Margin="12,12,12,12" MaxHeight="64" MaxWidth="64"/>
                            <Label Content="Ясно" FontSize="12" Foreground="#FFEFEFEF" HorizontalAlignment="Center"></Label>
                        </StackPanel>
                    </Viewbox>
                </Button.Content>
            </Button>
            <Button x:Name="CloudButton" Grid.Column="1">
                <Button.Content>
                    <Viewbox MaxHeight="128">
                        <StackPanel>
                            <Image Source="/Images/weather/cloudy_pic.png" Margin="12,12,12,12" MaxHeight="64" MaxWidth="64"/>
                            <Label Content="Облачно" FontSize="12" Foreground="#FFEFEFEF" HorizontalAlignment="Center"></Label>
                        </StackPanel>
                    </Viewbox>
                </Button.Content>
            </Button>
            <Button x:Name="RainButton" Grid.Column="2">
                <Button.Content>
                    <Viewbox MaxHeight="128">
                        <StackPanel>
                            <Image Source="/Images/weather/rainy_pic.png" Margin="12,12,12,12" MaxHeight="64" MaxWidth="64"/>
                            <Label Content="Дождь" FontSize="12" Foreground="#FFEFEFEF" HorizontalAlignment="Center"></Label>
                        </StackPanel>
                    </Viewbox>
                </Button.Content>
            </Button>
            <Button x:Name="SnowButton" Grid.Column="3">
                <Button.Content>
                    <Viewbox MaxHeight="128">
                        <StackPanel>
                            <Image Source="/Images/weather/snowy_pic.png" Margin="12,12,12,12" MaxHeight="64" MaxWidth="64"/>
                            <Label Content="Снег" FontSize="12" Foreground="#FFEFEFEF" HorizontalAlignment="Center"></Label>
                        </StackPanel>
                    </Viewbox>
                </Button.Content>
            </Button>
            <Button x:Name="DustButton" Grid.Column="4" Margin="12,12,12,12">
                <Viewbox MaxHeight="128">
                    <StackPanel>
                        <Image Source="/Images/weather/wind_pic.png" Margin="12,12,12,12" MaxHeight="64" MaxWidth="64"/>
                        <Label Content="Пустыня" FontSize="12" Foreground="#FFEFEFEF" HorizontalAlignment="Center"></Label>
                    </StackPanel>
                </Viewbox>
            </Button>
            <Button x:Name="MegaRainButton" Grid.Column="5" Margin="12,12,12,12">
                <Viewbox MaxHeight="128">
                    <StackPanel>
                        <Image Source="/Images/weather/rainy_pic.png" Margin="12,12,12,12" MaxHeight="64" MaxWidth="64"/>
                        <Label Content="Сильный дождь" FontSize="12" Foreground="#FFEFEFEF" HorizontalAlignment="Center"></Label>
                    </StackPanel>
                </Viewbox>
            </Button>
            <Button x:Name="MegaSnowButton" Grid.Column="6" Margin="12,12,12,12">
                <Viewbox MaxHeight="128">
                    <StackPanel>
                        <Image Source="/Images/weather/snowy_pic.png" Margin="12,12,12,12" MaxHeight="64" MaxWidth="64"/>
                        <Label Content="Сильный снег" FontSize="12" Foreground="#FFEFEFEF" HorizontalAlignment="Center"></Label>
                    </StackPanel>
                </Viewbox>
            </Button>
            <Button x:Name="MegaDustButton" Grid.Column="7" Margin="12,12,12,12">
                <Viewbox MaxHeight="128">
                    <StackPanel>
                        <Image Source="/Images/weather/wind_pic.png" Margin="12,12,12,12" MaxHeight="64" MaxWidth="64"/>
                        <Label Content="Сильная пустыня" FontSize="12" Foreground="#FFEFEFEF" HorizontalAlignment="Center"></Label>
                    </StackPanel>
                </Viewbox>
            </Button>
        </Grid>
        <TextBlock Grid.Column="1" VerticalAlignment="Bottom" TextAlignment="Center" FontSize="22" TextWrapping="Wrap" Text="Панель управления погодными условиями на карте" Foreground="White"/>
    </Grid>
</Page>
